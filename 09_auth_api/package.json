{
  "name": "auth_api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:w": "set POSTGRES_PORT=5432&& set POSTGRES_HOST=localhost&& set POSTGRES_USER=postgres&& set POSTGRES_PASSWORD=123&& set POSTGRES_DB=nestjs&& nodemon node index.js",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
